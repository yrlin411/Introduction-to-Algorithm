#include <iostream>
#include <stack>
using namespace std;

int main() {
    
    cin.tie(0);
    cin.sync_with_stdio(0);
    
    int num;
    stack<int> mStack;
    
    while (cin >> num) {
        
    }
    
    return 0;
}
